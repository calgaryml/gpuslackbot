{
    "display_information": {
        "name": "GPU Bot",
        "description": "Bot to monitor GPU usage",
        "background_color": "#000000"
    },
    "features": {
        "bot_user": {
            "display_name": "GPU Bot",
            "always_online": false
        },
        "slash_commands": [
            {
                "command": "/gpus_HOSTNAME",
                "description": "Show GPU usage on Server",
                "should_escape": false
            }
        ]
    },
    "oauth_config": {
        "scopes": {
            "bot": [
                "commands"
            ]
        }
    },
    "settings": {
        "interactivity": {
            "is_enabled": true
        },
        "org_deploy_enabled": false,
        "socket_mode_enabled": true,
        "token_rotation_enabled": false
    }
}